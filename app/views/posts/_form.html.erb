<div style ="box-shadow: 3px 3px 3px 3px solid black;text-align:center">

  <%= form_for(@post) do |f| %>
    <% if @post.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@post.errors.count, "error") %> prohibited this Article from being saved:</h2>

        <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div style = "text-align:center;display:inline-block">
      <%= f.label :title %><br>
      <%= f.text_field :title , class: "form-control", style: "width:70em"  %><br>
      <%= f.label :body , html: { validateRequest: false } %><br>
      <%= f.text_area :body , class: "form-control", rows: "5", id: "comment", style: "width:70em;height:30rem" %>
    </div>
    <div style = "height:3rem">
    </div>
    <div class="actions">
      <%= f.submit "Make new post", class: "btn btn-default" %>
    </div>
  <% end %>
</div>